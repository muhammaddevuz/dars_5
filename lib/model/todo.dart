class Todo {
  int id;
  String title;
  String description;
  DateTime date;
  bool isComplated;

  Todo(
      {required this.id,
      required this.title,
      required this.description,
      required this.date,
      required this.isComplated});
}
